let strawberryCount = 20

if(strawberryCount > 9){
    console.log('Congrats!')
} //else {
//    console.log('Sorry, we do not ship orders that small.')
//}

//the else is an optional that works as an insurance plan

let testVal = false

//an empty string value evals to false, while a used one ex 'string', evals to true

//console.log('There are 2 strawberries')

let StrawberryCount = 2

while(strawberryCount < 200){
    console.log(`There are ${strawberryCount} strawberries.`)
    strawberryCount = strawberryCount + 1
    //strawberryCount += 1
    //strawberryCount++
}

